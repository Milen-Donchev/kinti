apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.web.name }}-config
  namespace: {{ .Release.Namespace }}
data:
{{- range $key, $value := .Values.web.config }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
