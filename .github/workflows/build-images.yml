name: Build container images

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: build-images-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io
  OWNER: milen-donchev

jobs:
  build-api:
    name: Build API image
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push API image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: apps/api/Dockerfile
          push: true
          cache-from: type=gha,scope=kinti-api
          cache-to: type=gha,scope=kinti-api,mode=max
          tags: |
            ${{ env.REGISTRY }}/${{ env.OWNER }}/kinti-api:latest
            ${{ env.REGISTRY }}/${{ env.OWNER }}/kinti-api:${{ github.sha }}
          labels: |
            org.opencontainers.image.source=https://github.com/Milen-Donchev/kinti
            org.opencontainers.image.description=Kinti API
            org.opencontainers.image.revision=${{ github.sha }}

  build-web:
    name: Build Web image
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push Web image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: apps/web/Dockerfile
          push: true
          cache-from: type=gha,scope=kinti-web
          cache-to: type=gha,scope=kinti-web,mode=max
          tags: |
            ${{ env.REGISTRY }}/${{ env.OWNER }}/kinti-web:latest
            ${{ env.REGISTRY }}/${{ env.OWNER }}/kinti-web:${{ github.sha }}
          labels: |
            org.opencontainers.image.source=https://github.com/Milen-Donchev/kinti
            org.opencontainers.image.description=Kinti Web
            org.opencontainers.image.revision=${{ github.sha }}
